  /*import axios from 'axios';

  const API_KEY = 'your_omdb_api_key_here';
  
  // function to fetch movies from OMDB API
  const fetchMovies = async (searchTerm, page = 1) => {
    try {
      const response = await axios.get(`https://www.omdbapi.com/?s=${searchTerm}&page=${page}&apikey=${API_KEY}`);
      return response.data;
    } catch (error) {
      console.error(error);
      return null;
    }
  }
  
  export default fetchMovies;*/
  